const PI: f32 = radians(180.0);

struct Params {
    debug_wireframe_alpha: f32,
    debug_interpolate: f32,
    starburst_scale: f32,
    ray_dir: vec3f,
    bid: i32,
    intensity: f32,
    lambda: f32,
    wireframe: u32,
}